#include <stdio.h>
/**
 * main - entry
 *
 * Description: Printing Base 16
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 48; 1 < 58; i++)
	{
		putchar(i);
	}
	for (i = 93; i < 103; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
